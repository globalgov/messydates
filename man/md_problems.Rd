% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_input.R
\name{md_problems}
\alias{md_problems}
\title{Diagnose unparseable dates}
\usage{
md_problems(x)
}
\arguments{
\item{x}{A vector of dates, usually character.}
}
\value{
A data frame with one row per problematic element and the columns
\code{index} (its position in \code{x}), \code{input} (the offending value), and
\code{reason}. A zero-row data frame means every element can be coerced.
}
\description{
Reports which elements of a vector \code{as_messydate()} cannot represent, and
why. Where \code{as_messydate()} stops at the first problem, or returns \code{NA}
for text it could not read, \code{md_problems()} inspects every element and
returns one row per failure. This is intended for checking a column of
dates before coercing it.
}
\examples{
md_problems(c("2019-01-01", "2019-02-30", "2019-W12", "not a date"))
# a clean vector returns no rows
md_problems(c("2019-01-01", "2019-01"))
}
